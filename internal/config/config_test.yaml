server:
  http:
    host: "127.0.0.1"
    port: "8080"

logger:
  path: "/log/log.txt"
  level: "debug"

db:
  postgres:
    dialect: "postgres"
    dsn: ""
    maxConnLifetimeMinute: 3
    maxConn: 10

repository:
  type: "postgres"